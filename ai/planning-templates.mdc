---
description: Full planning templates and file structures. Read this when creating a new task plan — referenced by planning.mdc.
globs:
alwaysApply: false
---

# Planning Templates

This file contains templates for all task plan files. Referenced by `planning.mdc` when creating a new task.

---

## Directory Structure

```
$AI_AGENT_TASKS_FOLDER/{type}/{name}/
├── plan.md          # Current truth — always the latest version
├── changelog.md     # How the plan evolved over iterations
└── log.md           # Combined conversation + execution log
```

### Folder Naming (same as git branches)

| Type       | Pattern                          | Example                                      |
|------------|----------------------------------|----------------------------------------------|
| `feature/` | `{jira-ticket}-{description}`   | `feature/crk-1234-user-export`               |
| `adhoc/`   | `{prefix}-{description}`        | `adhoc/refactor-auth-flow`, `adhoc/doc-api`  |
| `bugfix/`  | `{jira-ticket}-{description}`   | `bugfix/crk-5678-null-avatar`                |

---

## plan.md Template

```markdown
# {Title}

**Status**: draft | in-progress | completed | abandoned
**Created**: {date}
**Updated**: {date}
**Ticket**: {link if available}

## Context

Why this work is needed. Link to tickets, PRs, or discussions if available.

## Scope

**In scope:**
- ...

**Out of scope:**
- ...

## Design

Key decisions, approach, and trade-offs. Keep it concise — this is a working document, not an RFC.

## Test Plan

### Acceptance Criteria
- [ ] Criterion 1
- [ ] Criterion 2

### Test Cases

**Happy path:**
- [ ] {test description}

**Edge cases:**
- [ ] {test description}

**Error cases:**
- [ ] {test description}

### Mocking Strategy
What needs to be mocked (APIs, services, modules) and at what level.

## Tasks

- [ ] Step 1
- [ ] Step 2
- [ ] Step 3

## Related Tasks

- `{type}/{name}` — {why it's related}

## Risks

- {Risk}: {mitigation}
```

### Guidelines

- **Status** reflects current state. Update it as work progresses.
- **Tasks** is the canonical TODO list. This is the single source of truth for what's done and what's pending.
- **Related Tasks** links to other plans in `$AI_AGENT_TASKS_FOLDER`. When starting a bugfix related to a feature, reference the feature plan here.
- **Design** should explain trade-offs: what alternatives were considered and why this approach was chosen.
- **Test Plan** feeds directly into TDD. Each test case becomes a Red-Green cycle.
- **Scope** prevents creep. If something is out of scope, it stays out — log it for a follow-up task.
- Keep plan.md as the **latest truth**. When the plan changes, update plan.md and record what changed in changelog.md.

---

## changelog.md Template

```markdown
# {Title} — Plan Changelog

## v1 — {date} (initial draft)

- {What was in the initial plan}

## v2 — {date} ({reason for change})

- {What changed and why}
```

### Guidelines

- One entry per meaningful plan revision.
- Record WHY the plan changed, not just what changed.
- Version numbers are simple incrementing integers (v1, v2, v3...).

---

## log.md Template

```markdown
# {Title} — Log

## Sessions

| # | Date       | Agent        | Session ID / Transcript        | Summary                            |
|---|------------|--------------|--------------------------------|------------------------------------|
| 1 | {date}     | {tool name}  | {session-id or transcript ref} | {one-line summary}                 |

---

## Session 1 — {date}

**Agent**: {tool name}
**Session**: {session ID or reference}

### Exchange 1 — {topic}

**User**
{The question or instruction the user gave}

**Agent**
{The agent's response — key analysis, recommendations, decisions}

**Log**
1. {Action taken — e.g., Read file X, Created file Y, Updated Z}
2. {Next action}
3. ...

### Exchange 2 — {topic}

**User**
{Follow-up, confirmation, or course correction}

**Agent**
{Response, updated approach}

**Log**
1. {Action taken}
2. ...

### Session Summary

**Decisions**: {Key decisions made and why}
**Discoveries**: {Unexpected behavior, edge cases found}
**Issues**: {Errors encountered and resolution}

---

## Session 2 — {date}

...
```

### Guidelines

- One `log.md` per task. Append a new `## Session N` section for each work session.
- Create `log.md` at the START of the first session. Update it incrementally as you work — don't wait until the end.
- Each exchange is a self-contained unit: what the user asked → what the agent decided → what the agent did. This keeps conversation and execution in natural chronological order.

### Sessions table

Quick reference for all sessions. Include enough to find and resume any session.
- Cursor: reference agent transcript ID from `~/.cursor/projects/.../agent-transcripts/`
- Claude Code: session name (use `claude --resume {name}`)
- Other tools: whatever identifier allows resuming

### What goes in each exchange

- **User**: The instruction, question, or feedback that triggered the exchange.
- **Agent**: The response — analysis, decisions, recommendations. Capture reasoning and trade-offs, not just "I did X."
- **Log**: Numbered list of actions taken (files read/written/edited, commands run). This is the audit trail.

### What does NOT go in the log

- Verbose terminal output (reference the command, not the full output)
- Full conversation transcripts — capture the exchanges that contain decisions, requirements, and reasoning. Skip routine acknowledgments.
- Routine status updates (check off tasks in `plan.md` instead)

---

## File Creation Order

When starting a new task:

1. Create the folder (after user confirms the name)
2. Create `plan.md` (draft status)
3. Create `changelog.md` (v1 entry)
4. Create `log.md` (first session entry + initial exchange)
5. Share plan with user → discuss → iterate → update plan.md and changelog.md
6. Once approved, change status to `in-progress` and begin execution
